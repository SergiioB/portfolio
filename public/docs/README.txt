Place a redacted public CV here, for example:

  public/docs/cv-public.pdf

Then enable the link in src/config/site.ts:
  PUBLIC_CV.enabled = true

Do not store private/source CV files in this repository.
Keep editable versions (DOCX/ODT) in a private folder outside git.
