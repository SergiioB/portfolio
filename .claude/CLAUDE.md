# Claude Agent Context Pointer

This workspace/project uses `AGENTS.md` as the source of truth so Codex, Zed Codex ACP, and other coding agents share one rule set.

Before editing:

1. Read `../AGENTS.md`.
2. Read the nearest nested `AGENTS.md` for the target folder.
3. Read `../.agentic/rules.md` and the active work-pack under `../.agentic/work-packs/`.

Do not print secrets from `.env`, local properties, keystores, API keys, Firebase files, or production data.
