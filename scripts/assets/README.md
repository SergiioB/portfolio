Asset generation helpers live here so the repository root stays focused on the Astro app.

- `svg/` contains one-off and iterative SVG generation scripts.

Only the generated output belongs in `public/`. Source scripts and working templates should stay under `scripts/`.
The active Open Graph image generator lives at `scripts/generate-og-image.mjs`.
